<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.canplimplam.restpolloloco">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".FichaProductoActivity"></activity>
        <activity android:name=".ListadoActivity" />
        <activity android:name=".LineaPedidoActivity" />
        <activity android:name=".AltaProductoActivity" />
        <activity android:name=".AltaCamareroActivity" />
        <activity android:name=".PedidoActivity" />
        <activity android:name=".ProductoActivity" />
        <activity android:name=".CamareroActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>